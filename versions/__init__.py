"""
Versioned frontends for the AI Animation Studio.

For now, the root `app.py` runs the latest stable version (v1).
Individual version folders (e.g. `versions/v1`) can store
snapshots or experimental variants of the UI.
"""


